const config = {
  theme: {
    extend: {
      colors: {
        base: {
          400: "var(--color-base-400)",
          500: "var(--color-base-500)",
          800: "var(--color-base-800)",
          900: "var(--color-base-900)",
        },
        "accent-focused": "var(--color-accent-focused)",
      },
    },
  },
};

export default config;
